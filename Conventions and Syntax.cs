/* 
Syntax affect how the code reads. If it is not correct then it won't run
correctly, such as missing a brace, an uppercase, period and such.
Debug.Log(transform.position.x) is an example of Syntax. 
if this was wrong, then it wouldn't work. there would be an error.
*/
/*Conventions is how the code is written, white spaces, formatting and tabbing
make it easy to read and looks pretty.  */  